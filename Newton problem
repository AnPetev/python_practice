def sqrt(x):
  root = 1
  while not guess_enough(root,n):
    return root
    
def guess_enough(root,target):
  if abs(root**3-target) < 0.001:
    return true
  else:
    return false
 def improve(root,target):
   return (root+(target/root))/3
